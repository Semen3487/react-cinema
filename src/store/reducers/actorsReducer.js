
const initialState = {
  actors: [],
  isFetching: false,
  error: null,
};

export default function actorsReducer(state = initialState, {type, payload}){
  switch(type){
    default: return state;
  }
};