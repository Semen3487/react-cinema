
const initialState = {
  directors: [],
  isFetching: false,
  error: null,
};

export default function directorsReducer(state = initialState, {type, payload}){
  switch(type){
    default: return state;
  }
};