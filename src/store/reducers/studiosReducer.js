
const initialState = {
  studios: [],
  isFetching: false,
  error: null,
};

export default function studiosReducer(state = initialState, {type, payload}){
  switch(type){
    default: return state;
  }
};